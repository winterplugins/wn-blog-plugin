# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: dimsog.blog::lang.models.tag.fields.name
        required: true
    slug:
        label: dimsog.blog::lang.models.tag.fields.slug
        preset:
            field: name
            type: slug
        required: true
