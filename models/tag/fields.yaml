# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: Название
        required: true
    slug:
        label: URL
        preset:
            field: name
            type: slug
        required: true
