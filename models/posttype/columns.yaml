# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: dimsog.blog::lang.models.posttype.columns.id
        searchable: true
    name:
        label: dimsog.blog::lang.models.posttype.columns.name
        searchable: true
