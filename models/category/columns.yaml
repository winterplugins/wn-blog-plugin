# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: dimsog.blog::lang.models.category.columns.id
        searchable: true
        width: '100px'
    name:
        label: dimsog.blog::lang.models.category.columns.name
        searchable: true
    count:
        label: dimsog.blog::lang.models.category.columns.count
        relation: posts
        useRelationCount: true
