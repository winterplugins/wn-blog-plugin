# ===================================
#  List Column Definitions
# ===================================

columns:
    tag_id:
        label: dimsog.blog::lang.models.posttag.columns.tag
        relation: tag
        select: name
        width: '250px'
